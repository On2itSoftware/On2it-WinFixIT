Drop sgdisk64.exe here (from gdisk-windows, GPL-licensed, https://sourceforge.net/projects/gptfdisk/)
to enable GPT partition naming. Optional -- Build-Your-USB.ps1 skips this step
with a warning if the file isn't present; volume labels are set either way.
